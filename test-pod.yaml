apiVersion: v1
kind: Pod
metadata:
  name: audit-test-pod
  namespace: default
spec:
  containers:
  - name: test-app
    image: nginx:alpine
    securityContext:
      privileged: true
      runAsNonRoot: false
